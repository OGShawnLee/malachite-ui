import Render from './Render.svelte';

export default Render;

export const elementTagNames = ['article', 'button', 'div', 'nav', 'p', 'section', 'span', 'ul'];
// I could add more but I'll trust the <svelte:element> implementation